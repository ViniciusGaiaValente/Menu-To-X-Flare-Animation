import 'package:flutter/material.dart';
import 'package:template/flare_animation/menu_to_x_app.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MenuToXApp();
  }
}